import React from "react"

function Navbar(){
    return(
        <nav>
            <span>Logo</span>
            <span>About</span>
            <span>Contact</span>
        </nav>
    );
}

export default Navbar